export class User {
    constructor(id: number, name: string, email: string) {
      this.id = id;
      this.userName = name;
      this.email = email;
    }

    id: number;
    userName: string;
    email: string;
  }
